---
# =============================================================================
# Ansible Inventory for Terrarium Git
# =============================================================================
# Edit this file to add your production servers
# =============================================================================

all:
  hosts:
    # Example: Add your production server(s) here
    # terrarium-git-prod:
    #   ansible_host: 192.168.1.100
    #   ansible_user: deploy
    #   ansible_ssh_private_key_file: ~/.ssh/id_rsa
    
    # For local testing (requires local SSH)
    # localhost:
    #   ansible_connection: local

  vars:
    # Global variables
    domain: terrarium-git.terrarium.network
    container_registry: containers.terrarium.network
